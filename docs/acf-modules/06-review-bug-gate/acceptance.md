# 06 验收标准

## 验收条件

- 每类正式评审都能产生 `ReviewRecord`
- QA 失败结果可以转为 `Bug`
- 非缺陷阻塞可以转为 `Blocker`
- Gate 未满足输入时不能通过
- Gate 通过结果必须携带代码锚点和输入状态

## 测试场景

1. 创建一条需求评审记录
2. 从失败验证中创建 `Bug` 并推进修复
3. 创建一条 `Blocker` 并等待用户回应
4. 尝试在输入不全时通过 Gate，验证失败
5. 输入满足后创建 `passed` 状态的 `GateDecision`
