# 02 验收标准

## 验收条件

- 可以从控制平面启动一条执行链路
- 可以消费并投影运行时事件
- 中断、失败、超时等情况有正式状态回写
- 调度接口不暴露底层 transcript 细节给上层业务模块

## 测试场景

1. 触发一次普通执行并产生事件流
2. 触发一次中断并验证回写状态
3. 触发一次失败并验证错误摘要落库
4. 校验 `TaskRun` 和运行时 session 的引用关系
